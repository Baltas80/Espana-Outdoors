# España Outdoor production integration configuration
# This file contains placeholders only. Never commit real secrets.

# Self-hosted/controlled Valhalla service.
VALHALLA_BASE_URL=https://routing.example.com/

# AEMET OpenData. Keep the key in the deployment secret manager.
AEMET_API_KEY=<set-in-secret-manager>
AEMET_SOURCE_BASE=https://opendata.aemet.es/opendata/api/

# Optional observability endpoints are configured by the deployment environment.
OTEL_EXPORTER_OTLP_ENDPOINT=<deployment-value>
SENTRY_DSN=<deployment-value>
